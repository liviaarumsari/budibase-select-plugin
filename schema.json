{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "select-component",
    "friendlyName": "select-component",
    "description": "Select component with search list",
    "icon": "Text",
    "settings": [
      {
        "type": "text",
        "key": "text",
        "label": "Label"
      },
      {
        "type": "select",
        "label": "Select Type",
        "key": "selectType",
        "defaultValue": "single",
        "showInBar": true,
        "barStyle": "picker",
        "options": [
          {
            "label": "Single-select",
            "value": "single"
          },
          {
            "label": "Multi-select",
            "value": "multiple"
          }
        ]
      }
    ]
  }
}